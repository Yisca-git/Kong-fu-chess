# Sound Assets

Place the following `.wav` files here to enable sound effects:

- `move.wav`    — played when a piece arrives at its destination
- `capture.wav` — played when a piece captures an enemy
- `jump.wav`    — played when a piece jumps

If a file is missing, that sound is silently skipped.
Only `.wav` format is supported (Windows `winsound` stdlib).
